# {{ .ProjectName }}

由 `go-web-starter` 生成，目标 Go 版本为 `{{ .GoVersion }}`。

## 启动前准备

1. 按需启动 Redis{{ if .MySQL }}、MySQL{{ end }}{{ if .MongoDB }}、MongoDB{{ end }}。
2. 检查 `config/config.yml`，不要把生产凭据写入或提交到配置文件。
3. 使用 `APP_` 环境变量覆盖敏感配置，例如：

```shell
export APP_REDIS_AUTH='replace-me'
{{- if .MySQL }}
export APP_DATABASE_PASSWORD='replace-me'
{{- end }}
{{- if .MongoDB }}
export APP_MONGODB_PASSWORD='replace-me'
{{- end }}
export APP_KEY_TYPE='jwt'
export APP_KEY_JWT_SECRET='replace-with-at-least-32-bytes'
```

环境变量名由配置路径转换而来：增加 `APP_` 前缀，并将 `.` 替换为 `_`。模板已显式绑定数据库、Redis、鉴权、Prometheus 和 Lark 的常用覆盖项。

## 运行

```shell
go mod tidy
go run ./app/main.go http -c ./config/config.yml
```

常用命令：

- `{{ .BinaryName }} http -c ./config/config.yml`
- `{{ .BinaryName }} version`
- `make test`：执行 race 测试
- `make vet`：执行静态检查
- `make build`：输出 `bin/{{ .BinaryName }}`

## 配置约束

- 配置使用严格解码；未知字段、错误 duration 或缺少必要字段会阻止启动。
- `key.type` 支持 `none`、`basic`、`key`、`jwt`。`none` 仅用于本地 debug。
- `debug: false` 时禁止使用 `key.type: none`，容器启动前必须通过配置或环境变量选择鉴权方式。
- `server.corsOrigins` 必须显式列出允许的 Origin。
- duration 使用 `5s`、`2m` 等字符串，不使用裸数字。
- 外部服务会在应用启动时执行连通性检查，失败会明确终止启动。

## 容器

先生成并提交 `go.sum`，再构建镜像：

```shell
go mod tidy
docker build -t {{ .BinaryName }}:dev .
```

镜像使用非 root 用户运行。容器内仍可通过 `APP_...` 环境变量覆盖配置。
